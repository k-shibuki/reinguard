# Minimal repo-local config (ADR-0008). See pkg/schema/reinguard-config.json.
schema_version: "0.3.0"
default_branch: main
providers:
  - id: git
    enabled: true
  - id: github
    enabled: true
    options: {}
