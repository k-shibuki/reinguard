# Dogfood route rules: resolve a route when state is resolved.
rules:
  - type: route
    id: when_resolved
    priority: 10
    route_id: proceed
    when:
      op: eq
      path: state.kind
      value: resolved
