This directory holds **repository-local** reinguard configuration (and in the
future, knowledge manifests referenced by `knowledge-manifest.yaml`).

Normative architecture decisions live in `docs/adr/`, not here. These files
describe how *this* repository configures `rgd` for its own workflows.
