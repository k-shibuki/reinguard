---
alwaysApply: true
---
# Workflow Policy (Adapter)

Follow [.reinguard/policy/workflow--pr-discipline.md](../../.reinguard/policy/workflow--pr-discipline.md) for Issue-driven work, PR discipline, branch naming, and exceptions.

## Command separation (review vs merge)

Treat **review**, **fix**, and **merge** as distinct steps (supports future `rgd` semantics):

- **Ready for review** → use **`pr-review`** (or human/CodeRabbit review only): judgment, no merge.
- **Unresolved threads / changes requested** → **`review-fix`**: address findings, disposition replies, resolve threads.
- **Review done, CI green, policy satisfied** → **`pr-merge`**: execute merge only after checks.

## Evidence targets

- Until `rgd observe` ships, see `evidence-temporary.mdc` for GitHub observation (`gh` / `git` read-only).

## Agent procedure reference

Commit, PR, and review procedures are defined in `.cursor/commands/`
(`implement.md`, `pr-create.md`, `pr-review.md`, `review-fix.md`, `pr-merge.md`). Use `gh` CLI for
observation (`gh issue view`, `gh pr view`, `gh pr checks`, etc.).

## Maintainer reference

Branch protection, labels, and CI gates: [.github/CONTRIBUTING.md](../../.github/CONTRIBUTING.md).
